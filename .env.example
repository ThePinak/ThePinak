# PINAK.OS Environment Configuration
# GitHub username to collect public data from
GITHUB_USERNAME=ThePinak

# Personal Access Token (PAT) for authenticated API rate limits (optional for low-volume public queries, recommended)
GITHUB_TOKEN=your-github-personal-access-token
